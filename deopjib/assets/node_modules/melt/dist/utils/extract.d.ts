import type { MaybeGetter } from "../types";
/**
 * Extracts the value from a getter or a value.
 * Optionally, a default value can be provided.
 */
export declare function extract<T, D extends T>(value: MaybeGetter<T>, defaultValue?: D): D extends undefined | null ? T : Exclude<T, undefined | null> | D;
